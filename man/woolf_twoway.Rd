% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/woolf_twoway.R
\name{woolf_twoway}
\alias{woolf_twoway}
\title{Weighted Two-Way (Tukey) Decomposition of Log Odds Ratios}
\usage{
woolf_twoway(
  x,
  weighted = TRUE,
  name = deparse(substitute(x)),
  responseName = "log odds ratio",
  varNames = NULL
)
}
\arguments{
\item{x}{A \eqn{2 \times 2 \times R \times C}{2 x 2 x R x C} array, as
used by \code{\link[=woolf_test]{woolf_test()}}.}

\item{weighted}{Logical. If \code{TRUE} (the default), fit by weighted least
squares using Woolf inverse-variance weights. If \code{FALSE}, fit an
ordinary (unweighted) Tukey mean-polish via \code{twoway::twoway()}.}

\item{name}{A label for the data, used in the \code{print}/\code{plot} titles.
Defaults to the deparsed expression passed as \code{x}.}

\item{responseName}{Label for the response (log odds ratio) axis.}

\item{varNames}{Character vector of length 2 giving row/column variable
names. Defaults to the strata variable names from \code{x}'s dimnames.}
}
\value{
An object of class \code{"twoway"} (see \code{twoway::twoway()}), with
components \code{overall}, \code{roweff}, \code{coleff}, \code{residuals}, \code{name},
\code{rownames}, \code{colnames}, \code{method}, \code{responseName}, \code{varNames},
\code{compValue}, \code{slope}, and \code{power}. The weighted fit additionally
includes \code{weights} (the Woolf weights used) and \code{fit} (the underlying
\code{lm} object).
}
\description{
For a \eqn{2 \times 2 \times R \times C}{2 x 2 x R x C} table, computes an
additive row + column decomposition of the stratum log odds ratios tested
by \code{\link[=woolf_test]{woolf_test()}}, following Tukey's two-way ("median polish"-style) fit
as implemented in the \pkg{twoway} package.
}
\details{
Unlike \code{twoway::twoway()},
which weights every stratum equally, \code{woolf_twoway()} weights each
stratum's log odds ratio by its inverse variance (the Woolf weight), so
that strata with more precise log odds ratios contribute more to the
fitted row and column effects.

\code{twoway::twoway()} has no \code{weight} argument, so the additive model
\eqn{y_{ij} = \mu + \alpha_i + \beta_j + \epsilon_{ij}} is instead fit
directly by weighted least squares (\code{stats::lm()} with \code{weights = w} and
sum-to-zero contrasts), where \eqn{y_{ij}} is the log odds ratio and
\eqn{w_{ij}} its inverse variance in stratum \eqn{(i,j)}, exactly as used
internally by \code{\link[=woolf_test]{woolf_test()}}. The resulting fit is repackaged as an
object of class \code{"twoway"}, so that \code{print.twoway()} and \code{plot.twoway()}
from \pkg{twoway} (\code{which = "fit"} or \code{"diagnose"}) work on it unchanged.

Set \code{weighted = FALSE} to instead get the ordinary (unweighted) Tukey
mean-polish fit, via \code{twoway::twoway()} directly, for comparison.

This is a purely descriptive decomposition: it does not attempt to
partition the \code{\link[=woolf_test]{woolf_test()}} homogeneity statistic itself (see
\code{issues/woolf.md} for why a naive row/column/residual split of that
statistic is not generally valid). It is intended for visualizing how the
log odds ratio varies additively (or not) across the row and column
stratifying variables.
}
\examples{
if (requireNamespace("twoway", quietly = TRUE)) {
  data(Fungicide, package = "vcdExtra")

  tw <- woolf_twoway(Fungicide)
  print(tw)
  plot(tw, which = "fit")
  plot(tw, which = "diagnose")

  # compare with the unweighted (plain mean-polish) decomposition
  tw0 <- woolf_twoway(Fungicide, weighted = FALSE)
  rbind(mean = tw0$roweff, Woolf = tw$roweff)
  rbind(mean = tw0$coleff, Woolf = tw$coleff)
}

}
\seealso{
\code{\link[=woolf_test]{woolf_test()}}, \code{twoway::twoway()}, \code{twoway::plot.twoway()}

Other association tests:
\code{\link[=CMHtest]{CMHtest()}},
\code{\link[=GKgamma]{GKgamma()}},
\code{\link[=HLtest]{HLtest()}},
\code{\link[=breslow_day_test]{breslow_day_test()}},
\code{\link[=woolf_test]{woolf_test()}},
\code{\link[=zero.test]{zero.test()}}
}
\concept{association tests}
